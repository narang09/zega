import { AllowDecimalsDirective } from './allow-decimals.directive';

describe('AllowDecimalsDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowDecimalsDirective();
    expect(directive).toBeTruthy();
  });
});
