import { AllowPositiveDecimalsDirective } from './allow-positive-decimals.directive';

describe('AllowPositiveDecimalsDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowPositiveDecimalsDirective();
    expect(directive).toBeTruthy();
  });
});
