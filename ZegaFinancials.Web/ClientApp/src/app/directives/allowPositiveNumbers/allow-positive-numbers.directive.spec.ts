import { AllowPositiveNumbersDirective } from './allow-positive-numbers.directive';

describe('AllowPositiveNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowPositiveNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
