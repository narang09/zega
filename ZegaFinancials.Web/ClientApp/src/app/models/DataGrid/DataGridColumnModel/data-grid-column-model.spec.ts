import { DataGridColumnModel } from './data-grid-column-model';

describe('DataGridColumnModel', () => {
  it('should create an instance', () => {
    expect(new DataGridColumnModel(null)).toBeTruthy();
  });
});
