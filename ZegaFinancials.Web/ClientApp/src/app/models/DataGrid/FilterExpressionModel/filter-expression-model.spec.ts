import { FilterExpressionModel } from './filter-expression-model';

describe('FilterExpressionModel', () => {
  it('should create an instance', () => {
    expect(new FilterExpressionModel(null)).toBeTruthy();
  });
});
